const express=require('express'),cors=require('cors'),dotenv=require('dotenv'),mongoose=require('mongoose'),session=require('express-session');dotenv.config();const app=express();app.use(cors());app.use(express.json({limit:'50mb'}));app.use(express.urlencoded({limit:'50mb',extended:true}));app.use(express.static('public'));app.use('/uploads',express.static('uploads'));app.use(session({secret:process.env.SESSION_SECRET||'cinematic99secret',resave:false,saveUninitialized:true,cookie:{secure:false,httpOnly:true,maxAge:24*60*60*1000}}));mongoose.connect(process.env.MONGODB_URI||'mongodb://localhost:27017/cinematic99',{useNewUrlParser:true,useUnifiedTopology:true}).then(()=>console.log('✓ MongoDB Connected')).catch(err=>console.log('✗ DB Error:',err));app.use('/api/auth',require('./routes/auth'));app.use('/api/packages',require('./routes/packages'));app.use('/api/videos',require('./routes/videos'));app.use('/api/payments',require('./routes/payments'));app.use('/api/admin',require('./routes/admin'));app.use('/api/courses',require('./routes/courses'));app.get('/api/health',(req,res)=>res.json({status:'running'}));app.get('*',(req,res)=>res.sendFile(__dirname+'/public/index.html'));app.listen(process.env.PORT||5000,()=>console.log('🎬 Cinematic 99 Server on port',process.env.PORT||5000));